{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug SimpleGUI Demo",
            "type": "gdb",
            "request": "launch",
            "target": "${workspaceFolder}/build/demo.exe",
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "build"
        }
    ]
}
