{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug SimpleGUI Demo",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/demo.exe",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "build"
        }
    ]
}
